<<module bro>>
takes input[lo:integer, hi:integer, arr:array[lo..hi] of integer];
returns [a:integer];
start
	a := 4;
end

<<<driver program>>>
start
	declare b:integer;
	b := 4 + 12 - 2;
	declare brr: array[2..10] of integer;
	[b] := use module bro with parameters b,b,brr;

end