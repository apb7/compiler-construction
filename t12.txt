** This testcase demonstrates the syntactic correctness of the constructs such as modules, simple and declaration statements, and expressions**

<<<driver program>>>
start
    declare flag, hj: boolean;
    declare a,b,c,ct: integer;
    declare arr: array[4..5] of integer;
    a := 25;
    b := 10;
    c := 5;
    ct:= 0;

    flag := a <=  b AND c <= a OR  (a<0 AND true);
    print(flag);
    a := 1;
    while(arr[a]<c)
    start
        ct:=0;
        while(ct<2)
            start
                ct:=ct+1;
                print(ct);
                a:=a+1;
                c:=c+1;9
            end

        print(c);
    end
    print(true);
    print(a);
end

**verify the output of the lexical analyzer as well**
**check the correctness of the parse tree**


